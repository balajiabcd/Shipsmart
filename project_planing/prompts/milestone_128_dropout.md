# Milestone #128: Implement Dropout Layers

**Your Role:** ML Engineer 2

Add dropout layers to neural networks for regularization (0.3 dropout rate recommended). Ensure dropout is applied during training but disabled during inference. Commit.